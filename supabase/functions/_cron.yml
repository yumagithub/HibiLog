# Supabase Edge Functions Cron Configuration
# This file configures scheduled execution of Edge Functions on Supabase Platform
# Note: Cron jobs only work in production (Supabase Platform), not in local development

# Run hunger-check function every hour
- name: "hourly-hunger-check"
  schedule: "0 * * * *" # Every hour at minute 0
  function: "hunger-check"
  method: "POST"

# Run daily-reminder function every hour (checks configured time)
- name: "hourly-daily-reminder"
  schedule: "0 * * * *" # Every hour at minute 0
  function: "daily-reminder"
  method: "POST"

# Run random-prompt function once per day at a random-ish time (11:30)
- name: "daily-random-prompt"
  schedule: "30 11 * * *" # Every day at 11:30
  function: "random-prompt"
  method: "POST"
